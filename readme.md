# LogiTracks Monorepo

## Stage 4 status

Stage 4 backend API layer is implemented under `backend/` with:

- full CRUD endpoints for master + operational entities,
- list pagination/sorting/filtering,
- dashboard/summary endpoints,
- simulation preview enrichment,
- typed Pydantic schemas and service-layer architecture,
- central config, structured logging, CORS, `.env.example`, Makefile.

See detailed backend docs in [`backend/README.md`](backend/README.md).
